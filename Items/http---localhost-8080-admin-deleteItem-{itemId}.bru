meta {
  name: http://localhost:8080/admin/deleteItem/{itemId}
  type: http
  seq: 5
}

delete {
  url: http://localhost:8080/admin/deleteItem/{itemId}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
}
